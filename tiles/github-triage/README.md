# GitHub Triage

A **read_only** tile over the [GitHub connector](../../connectors/github). Give it
an `owner/repo` and it summarizes and prioritizes the open issues. It never
writes.

Requires `GITHUB_PERSONAL_ACCESS_TOKEN` (see the connector README); until it's
set, the tile shows **needs token** on the board.
