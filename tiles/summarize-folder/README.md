# Summarize Folder

A **read_only**, zero-credential tile over the [local-files
connector](../../connectors/local-files). Give it a folder and it tells you
what's inside — file types and the gist of any text files. It never changes
anything.

Point the connector at a real folder (e.g. your notes or Downloads) by editing
`connectors/local-files/manifest.yaml`.
