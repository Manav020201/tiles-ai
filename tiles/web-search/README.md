# Web Search

A **read_only** tile over the [Web Search connector](../../connectors/web-search).
Type a query; it searches the web and returns a short digest with sources. It
never writes.

Requires `BRAVE_API_KEY` (see the connector README); until it's set, the tile
shows **needs token**.
