# What did I miss (Slack)

A **read_only** tile over the [Slack connector](../../connectors/slack). Give it a
channel and it summarizes recent activity, leading with anything that needs your
reply. It never posts.

Requires `SLACK_BOT_TOKEN` and `SLACK_TEAM_ID` (see the connector README).
