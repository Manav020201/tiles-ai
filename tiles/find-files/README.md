# Find Files

A **read_only**, zero-credential tile over the [local-files
connector](../../connectors/local-files). Type a filename or keyword; it lists
matching files (searched recursively under the connector's root). It never
changes anything.
