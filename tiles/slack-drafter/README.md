# Message Drafter (Slack)

A **draft**-tier tile over the [Slack connector](../../connectors/slack). Give it
`#channel: what to say`; it reads recent context, drafts a message, and queues
the post for your approval. Nothing posts until you approve it.

Requires `SLACK_BOT_TOKEN` and `SLACK_TEAM_ID` (see the connector README).
