# Research

A **read_only** tile over the [Web Search connector](../../connectors/web-search).
Ask a question; it searches the web and answers, grounded in and citing the
sources it found. It never writes.

Requires `BRAVE_API_KEY` (see the connector README).
