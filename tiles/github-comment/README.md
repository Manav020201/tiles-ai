# GitHub Comment

A **draft**-tier tile over the [GitHub connector](../../connectors/github). Give it
`owner/repo#issue: what to say`; it reads the issue, drafts a comment, and queues
the post for your approval. Nothing is posted until you approve it.

Requires `GITHUB_PERSONAL_ACCESS_TOKEN` with `repo` scope (see the connector
README).
