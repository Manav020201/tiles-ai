"""Translate — render text in another language. Behavior is in the manifest."""

from tiles_ai.handlers import PromptTile


class Translate(PromptTile):
    """Translate the user's text with the default brain."""
