# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local secrets / brain store (never commit API keys)
*.local.yaml
*.local.json
brain.local.*
.env

# Node / frontend (phase 5)
node_modules/
frontend/dist/
*.tsbuildinfo

# Board bundled into the package at release time (generated, not committed)
src/tiles_ai/web/

# OS
.DS_Store
.claude/settings.local.json

# Local handoff notes (not published)
HANDOFF.md
